const setItemList = (items) => ({
   type: 'ITEM_LIST',
   payload: items,
});

// export const loadingStatus = (status) => ({
//     type: 'LOADING',
//     payload: status,
// });

export default setItemList;
